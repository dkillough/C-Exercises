#include <math.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define BOOL char
#define FALSE 0
#define TRUE 1

// "BASIC"
void one(), two(), three(), four(), five(), six(), seven(), eight(), nine(),
    ten();
int helpTen(int input);

// "ADVANCED"
void eleven(), twelve(), thirteen();
void helpThirteen();
